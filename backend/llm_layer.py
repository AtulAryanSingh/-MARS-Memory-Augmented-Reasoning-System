def llm_generate(query: str, context: str):
    """
    Placeholder LLM layer (no API yet)
    Later: replace with OpenAI / local model
    """

    # For now simulate "LLM-style" answer
    return f"[LLM Enhanced Answer]\n\nBased on memory: {context}\n\nAnswer: {query}"